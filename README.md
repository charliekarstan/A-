# A* Algorithm Visualization & Performance Analysis

This repository contains a Python implementation of the A* pathfinding algorithm,
including visualization and performance diagnostics.

## Requirements
- Python 3.9 or newer

## How to run (copy & paste)

Open a terminal **inside the downloaded project folder** and run:
```bash

python3 -m pip install --upgrade pip
python3 -m pip install -r requirements.txt
python3 main3.py
